[project]
name = "dlss-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pefile>=2024.8.26",
    "platformdirs>=4.11.8",
    "pyside6>=6.11.2",
    "requests>=2.34.2",
]

[project.scripts]
dlss-manager = "dlss_manager:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
